/**
 * Remnant of the old rung-ladder. The office is now assembled from the value
 * spine (lib/resolve.ts) and adapted daily by the slider, so the ladder's
 * authored steps are gone; only the rung bounds remain, kept because the
 * stored RuleState still carries a `rung` field.
 */

export const FIRST_RUNG = 0;
export const LAST_RUNG = 5;
